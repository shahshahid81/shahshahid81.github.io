{
  "scripts": {
    "build:tailwind": "npx tailwindcss -i ./css/tailwind.input.css -o ./css/tailwind.min.css --minify",
    "build": "npm run build:tailwind && npx parcel build --no-source-maps index.html"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.16.4",
    "@parcel/transformer-webmanifest": "^2.16.4",
    "parcel": "^2.0.0-beta.2",
    "parcel-plugin-purgecss": "^2.0.0",
    "tailwindcss": "^4.2.1"
  }
}
